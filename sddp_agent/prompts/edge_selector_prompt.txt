You are evaluating whether a child node's hypothesis is supported by the analysis results.

## Current graph node
ID: {current_node_id}
Label: {current_node_label}

## Child node being evaluated
ID: {child_node_id}
Label: {child_node_label}
Expected state (child's hypothesis): {child_expected_state}
Description: {child_description}

## Case metadata (use when tool results are absent)
{case_metadata}

## Tool results collected to test this hypothesis
{tool_results}

## Decision
Does the evidence support the child's hypothesis?

- Answer "true" if the data (or case metadata, when no tools were run) is consistent with
  what the child node expects to find.
- Answer "false" if the data contradicts the hypothesis or if nothing supports it.
- When tool_results is "[]" (no tools ran for this child), base your decision ONLY on the
  case metadata above. If the case metadata contains information that directly confirms or
  refutes the hypothesis, use it. Otherwise return false so the fallback priority-1 path
  is taken.

Return ONLY valid JSON:
{{"holds": true_or_false, "reasoning": "<one sentence explaining the decision>"}}
