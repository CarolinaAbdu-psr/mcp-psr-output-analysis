You are evaluating whether a child node's hypothesis is supported by the analysis results.

## Current graph node
ID: {current_node_id}
Label: {current_node_label}

## Child node being evaluated
ID: {child_node_id}
Label: {child_node_label}
Expected state (child's hypothesis): {child_expected_state}
Description: {child_description}

## Case metadata (use when tool results are absent)
{case_metadata}

## Available CSV files in the case output folder
{catalog_summary}

Each entry shows: filename | chart_type | title | columns.
Use this to check whether relevant data files exist even when no tools were run.

## Tool results collected to test this hypothesis
{tool_results}

## Decision
Does the evidence support the child's hypothesis?

Evaluate in this order:

1. **Tool results available** (tool_results is not "[]"): base the decision primarily on
   the tool data. Catalog and case metadata are supplementary.

2. **No tools ran** (tool_results is "[]") — use the following instead:
   a. Check the CSV catalog above: if it contains files whose names or titles suggest they
      are relevant to the child's expected_state, treat that as positive evidence.
   b. Check case_metadata: for hypotheses about binary variables or non-convexities,
      look at the "Non-Convexities" key directly.
   c. If both catalog and metadata are inconclusive, return false so the priority-1
      fallback path is taken.

Return ONLY valid JSON:
{{"holds": true_or_false, "reasoning": "<one sentence citing which evidence drove the decision>"}}
